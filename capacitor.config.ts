import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.raianegoisbrandao.dgpromoter',
  appName: 'DG Promoter',
  webDir: 'www'
};

export default config;
